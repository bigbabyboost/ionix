name: "CI"

on:
  pull_request:
  push:
    branches: [master]
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 0" # weekly on Sunday
    - cron: "0 0 25 * *" # monthly on the 25th

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: wimpysworld/nothing-but-nix@main

      - uses: DeterminateSystems/nix-installer-action@main
        with:
          github-token: ${{ secrets.GH_TOKEN }}
          extra-conf: |
            experimental-features = nix-command flakes pipe-operators

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - uses: cachix/cachix-action@v16
        with:
          name: xaolan
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
          extraPullNames: pre-commit-hooks,nix-community
          skipPush: ${{ github.ref != 'refs/heads/main' || github.event_name != 'push' }}

      # Monthly flake.lock update
      - name: Update flake.lock (Monthly)
        if: ${{ github.event_name == 'schedule' && github.event.schedule == '0 0 25 * *' }}
        uses: DeterminateSystems/update-flake-lock@v25
        with:
          token: ${{ secrets.GH_TOKEN }}
          inputs: nixpkgs system disko niri nix-index-db nix-gaming zen-browser flake-parts hm hyprland hyprland-contrib hyprland-plugins hyprlock hyprpaper nix-yazi-flavors nvix plasma-manager yazi quickshell tailray hypridle pre-commit-hooks
          branch: monthly-lock
          git-author-name: "bigbabyboost"
          git-author-email: "ah0199004@gmail.com"
          git-committer-name: "bigbabyboost"
          git-committer-email: "ah0199004@gmail.com"
          commit-msg: "chore(lockfile): update flake.lock"
          pr-title: "chore(lockfile): update flake.lock"
          pr-labels: |
            dependencies
            automated
            monthly

      # Weekly flake.lock update
      - name: Update flake.lock (Weekly)
        if: ${{ github.event_name == 'schedule' && github.event.schedule == '0 0 * * 0' }}
        uses: DeterminateSystems/update-flake-lock@v25
        with:
          token: ${{ secrets.GH_TOKEN }}
          inputs: nixpkgs system disko niri nix-index-db nix-gaming zen-browser flake-parts hm hyprland hyprland-contrib hyprland-plugins hyprlock hyprpaper nix-yazi-flavors nvix plasma-manager yazi quickshell tailray hypridle pre-commit-hooks
          branch: weekly-lock
          git-author-name: "bigbabyboost"
          git-author-email: "ah0199004@gmail.com"
          git-committer-name: "bigbabyboost"
          git-committer-email: "ah0199004@gmail.com"
          commit-msg: "chore(lockfile): update flake.lock"
          pr-title: "chore(lockfile): update flake.lock"
          pr-labels: |
            dependencies
            automated
            weekly

      # Show flake outputs
      - run: nix flake show

      # Optional: Build something from your flake like a devShell or system config
      - name: Build NixOS configuration
        run: nix build ".#nixosConfigurations.${HOSTNAME}.config.system.build.toplevel"
        env:
          HOSTNAME: ionia # replace with your actual hostname key
      - name: Info
        run: nix path-info -Sh ./result

      # Optional: Build dev shell to ensure it works
      - name: Build devShell
        run: nix develop --command true
